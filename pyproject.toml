[project]
name = "todo-app"
version = "0.1.0"
description = "In-memory Todo console application for hackathon learning"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
todo = "src.main:main"

[tool.uv]
dev-dependencies = []
